<Window x:Class="MyWpfApp46.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyWpfApp46"
        mc:Ignorable="d"
        Title="プログラムランチャー" Height="450" Width="800" Background="#F3F3F3">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- カテゴリー一覧 -->
        <Border Grid.Column="0" Background="White" CornerRadius="5" Padding="5">
            <StackPanel x:Name="CategoryPanel">
                <TextBlock Text="カテゴリー" FontSize="16" FontWeight="Bold" Margin="5,0,0,10" Foreground="#333333"/>
                <!-- カテゴリーボタンがここに動的に追加される -->
            </StackPanel>
        </Border>

        <!-- プログラム一覧 -->
        <Border Grid.Column="1" Margin="10,0,0,0" Background="White" CornerRadius="5" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <WrapPanel x:Name="ProgramPanel">
                    <!-- プログラムボタンがここに動的に追加される -->
                </WrapPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
